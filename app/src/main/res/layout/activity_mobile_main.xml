<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_mobile_main.xml
    Layout para MobileMainActivity — pantalla completa WebView con:
    - ProgressBar de carga centrada
    - Overlay de error con botón Reintentar
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobileRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/luki_dark"
    tools:context=".mobile.MobileMainActivity">

    <!-- ── WebView principal ────────────────────────────────────────────── -->
    <WebView
        android:id="@+id/webView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ── Indicador de carga ─────────────────────────────────────────── -->
    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyle"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:indeterminate="true"
        android:indeterminateTint="@color/luki_cyan"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ── Overlay de error (inicialmente oculto) ────────────────────── -->
    <LinearLayout
        android:id="@+id/errorLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="@dimen/screen_margin_horizontal"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:visibility="gone">

        <TextView
            android:id="@+id/tvErrorMsg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/error_no_internet"
            android:textColor="@color/luki_text_primary"
            android:textSize="@dimen/text_h4"
            android:gravity="center"
            android:layout_marginBottom="@dimen/space_sm"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvErrorDetail"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/error_no_internet_detail"
            android:textColor="@color/luki_text_secondary"
            android:textSize="@dimen/text_body"
            android:gravity="center"
            android:layout_marginBottom="@dimen/space_lg" />

        <Button
            android:id="@+id/btnRetry"
            android:layout_width="match_parent"
            android:layout_height="@dimen/button_height_primary"
            android:text="@string/retry"
            android:backgroundTint="@color/luki_focus"
            android:textColor="@color/luki_dark"
            android:textStyle="bold"
            android:textSize="@dimen/text_body"
            android:focusable="true"
            android:contentDescription="@string/retry" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
